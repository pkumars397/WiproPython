# remove all the commas s
def removeBraceComma(l):
    newL=" ".join(l) #what join does is that it takes a list and iterate over each element and add to string.
    return newL

print(removeBraceComma(["one","two","three"]))